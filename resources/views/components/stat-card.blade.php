@props(['label', 'value'])

<div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
    <p class="text-sm font-medium text-slate-500">{{ $label }}</p>
    <p class="mt-3 text-4xl font-semibold tracking-tight text-slate-900">{{ $value }}</p>
</div>
